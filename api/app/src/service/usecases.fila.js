let database = []

const fila = {
    interacao(a){
        switch (a) {
            case '':
              return
              default:
                return "Método inválido"
          }
    }
}

module.exports = fila